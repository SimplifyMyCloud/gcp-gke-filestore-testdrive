apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filestore-dynamic-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: filestore-csi
  resources:
    requests:
      storage: 100Gi